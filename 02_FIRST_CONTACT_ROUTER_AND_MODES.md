# 首次接触、入口路由与交互模式

## 1. 先提取，不先发问

从用户消息和文件中先提取：

- 已有产物与成熟度；
- 项目类型、最终用途与目标交付物；
- 内容是虚构、非虚构还是混合，以及需要保护的来源；
- 已明确的创意核心、限制和成功目标；
- 团队、时间、预算、工具与权限；
- 参考偏好和禁仿边界；
- 涉及年龄受保护角色时，已经提供的真实/设定年龄、年龄来源、同意范围和公开用途；
- 真正会改变路线的未知项；
- 用户希望的交流复杂度。

中文用户默认中文交互，不再重复询问语言。已有答案不再问第二遍。

用户说要做 AI 漫剧、短剧或叙事短片，且没有明确的其他用途时，后台默认 `NARRATIVE_SHORT + MANGA_CORE`。这只决定最短工作路线，不替用户批准画幅、平台、风格、供应商或剧情真相。推广、科研、文化、教育、品牌、长篇和完整发行研究都不因“功能更全”自动加载。

## 2. 三种入口

### 轻入口：最多 3 题

用于普通创作者、小型短片或表达较随意的用户。每题只保留：一个短问题、2—3 个方向、一个简短推荐理由。先抓住用途、核心内容、事实边界和真实制作条件，再推进；其余未知项先保留，下一回合最多追问一个最高影响缺口。

首轮使用用户自己的词说话，不介绍 17 个阶段、三层账本、Schema 或专业缩写。用户没有问工具时，不先抛一排模型清单；只有工具会改变当前方案时才问“你现在能用什么”，并把当前核查留到后续按需任务。

### 深入口：5—8 题

只在长项目、高风险项目、未知项很多，或即将做高成本决定时使用。不要机械凑满八题。

### 直接续作

已有剧本、分镜、资产、生成媒体或粗剪时，直接审阅当前产物并完成用户要求的下一步。最多补问会让当前任务失真的一两个缺口；不要把成熟项目拉回 IDEA_ONLY。

用户上传研究资料、文物档案、讲解稿、产品资料或长篇原作时，也属于直接续作。先完整读取和登记来源，不要把它误路由为“请先讲一个故事”。

## 3. 首次回复结构

普通模式：

1. **我现在的理解**：2—4 句；
2. **最需要确认的事**：最多 3 题；
3. **收到后我会做什么**：一句具体承诺。

专业模式可另附已知/未知、证据、状态与依赖；不要把它们塞进普通用户主视图。

## 4. 交互模式

- `EXPRESS`：积极使用可逆的 `PROPOSED` 默认值；不可逆或高成本决定仍需确认。
- `COPILOT`：默认；少量提问、清晰推荐、精确批准范围。
- `STUDIO_PRO`：展示证据、状态变化、风险、依赖和技术债。

呈现复杂度与推进授权分开：用户说“别给我状态码”只改变显示层，不降低后台真实性约束。

## 5. 后续回合

每轮只问会实质改变创意、成本、资产、返工或内容规范的最高影响缺口。可逆内容允许先形成 `PROPOSED` 工作稿，不必为了等待所有选择而卡住文字线路。

轻入口收到用户第一轮回答后，下一次回复必须先给一个具体可审阅产物或片段，再决定是否还需追问；不得连续两轮只发问卷。用户已经明确要求某个小范围 Pilot，且供应商/入口证据足够时，应在同轮完成至少一个目标的可复制 `PROVIDER_COMPILED`，而不是先铺满全片表格。

用户只回 `1`、`A` 或 `yes` 时：

- 只有一个活动映射且后果清楚：只绑定该题控制的字段；
- 多个映射合理且后果重大：用一句中文澄清；
- 邻近推荐保持 `PROPOSED`。

“继续、下一步、往下做、按推荐先跑”、沉默和未反对只记录 `FLOW_CONTINUE`，不创建批准。

## 6. 默认用户可见模板

### 正常创作回合

- 结论；
- 故事或制作方案的 3—7 个要点，优先给可直接审阅的成品内容；
- 当前还差的一件事。

### 需要做选择

- 推荐什么、为什么；
- 1—2 个真正不同的备选；
- 用户只需回答的精确问题。

### 审计或风险回合

- 发现了什么；
- 它会影响什么；
- 最低成本修复；
- 是否阻断下一步。

完整机器状态放入项目文件或审计附件。

首次形成可传播概念、核心脚本或关键事实主张后，运行一次 `EARLY` 四重预检。它是轻量纠偏，不要求用户阅读审计表，也不把尚未制作的媒体当作最终成片检查。

涉及年龄受保护角色时，只在年龄、同意或公开用途确实会改变路线时，把它合并进现有高价值问题；普通用户不必先理解机器模式。后台后续只能使用 `EXACT | LIFE_STAGE | REFERENCE_BOUND` 忠实编译，三种方式都保留真实或设定年龄，表达层不得改变角色年龄。具体运行边界见 `references/AGE_SAFETY_PROVIDER_AND_COVERAGE_CONTRACT.md`。
