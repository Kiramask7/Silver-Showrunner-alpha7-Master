# Silver-Showrunner-alpha.7-Master v1.1.3｜1分钟说明使用手册

## 它有什么用

把小说、剧本、创意或已有分镜交给它，它会继续完成剧情梳理、剧本检查、角色与场景方向、导演分镜、逐镜视频提示词、连续性检查和制作进度记录。

它不会为了省字而删掉关键剧情、对白、动作、声音和镜头衔接。每条可复制提示词只负责一个镜头，并写清人物怎么演、镜头怎么看、声音何时出现、最后停在哪个画面。

## 最简单的用法

把文件交给 AI 后，直接说：

> 请使用 Silver-Showrunner-alpha.7-Master v1.1.3，完整读取我的文件。先用普通中文告诉我你理解了什么，再从当前进度继续。先完成剧情拆解、分镜和可直接复制的视频提示词，不要缩减原有对白。

看完结果后，只要说“继续”“修改第几镜”或“按我的意见重做这一段”。

## 应该选哪个文件

- 支持 ZIP 或 `.skill`：使用同名 ZIP 或 `.skill`，二者内容完全相同。
- 只能读取一个 Markdown：小说改编用 `LONGFORM-ONEFILE.md`；漫剧和短剧用 `MANGA_CORE-ONEFILE.md`；只做文字测试用 `TEXT_ONLY_ECO-ONEFILE.md`；需要全部能力用 `FULL-ONEFILE.md`。
- GitHub 根 `SKILL.md` 是模块入口，不是全能力单文件。
- 不要把 ZIP、`.skill` 和 ONEFILE 同时交给同一个 AI。

## 你只需要注意四件事

- 有附件时先让它完整读取，不要只贴零散片段。
- 想保留原话时明确说“对白逐字保留”。
- 修改时直接指出场景或镜头，不需要理解内部编号。
- 本包完成的是文字和流程检查；真正的视频效果仍要在生成后观看确认。
