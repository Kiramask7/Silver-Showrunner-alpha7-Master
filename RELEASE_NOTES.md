# Silver-Showrunner Alpha.7 Master v1.1.3

这是本地测试通过的纯净重构候选。

## 本版重点

- 公开参考文件只保留功能名称，不再展示开发阶段编号。
- 路由、状态兼容、提示词合同和长篇流程都已同步迁移。
- 修复多个 ONEFILE 剖面缺少模型可读依赖的问题。
- `FULL` 现在包含全部模型可读运行模块，共 50 个来源。
- GitHub 根 `SKILL.md` 恢复为真实模块入口；ONEFILE 只作为 Release 下载文件。
- 禁用内容门禁完成 97/97 变异检查，命中信息不回显原文。

## 发布资产

- `.skill` 与 ZIP：完整运行包，内容逐字节一致。
- `MANGA_CORE`：AI 漫剧、短剧核心创作。
- `LONGFORM`：小说和长篇连续改编。
- `TEXT_ONLY_ECO`：只跑纯文字导演测试。
- `AUX`：研究、策划和辅助创作路线。
- `MEDIA_RELEASE`：观察、修复、后期和发布准备。
- `FULL`：全部模型可读创作能力。

## 本地验证

- 源树与运行包检查通过。
- 路由注册表 54/54 通过。
- 公开修订名、禁用内容、路径、正文、归档成员和六种 ONEFILE 检查通过。
- ZIP 与 `.skill` 字节一致，安全解压后运行冒烟通过。
- 官方 Skill 入口检查通过。

没有真实生成并观看媒体，Production Validation 保持 `NOT_TESTED`。
